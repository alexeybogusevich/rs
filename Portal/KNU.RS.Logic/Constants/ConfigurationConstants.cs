namespace KNU.RS.Logic.Constants
{
    public static class ConfigurationConstants
    {
        // Authentication
        public const string CookieV2AuthenticationScheme = nameof(CookieV2AuthenticationScheme);
        public const string CookieTokenName = ".AspNetCore.Application.Id";

        // Sections
        public const string Emailing = nameof(Emailing);
        public const string Token = nameof(Token);
    }
}
